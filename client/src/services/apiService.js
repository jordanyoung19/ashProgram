

export class apiService {
    constructor() {
        this.baseURI = "http://localhost:9000";
    }

    // This will be a private method to make api calls to the 
    // back end 
    callAPI(route, ) {
        fetch()
    }

    // method to post song to database
    postSong(title, artist) {
    }
}